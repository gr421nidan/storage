import {ReactNode} from 'react';
import StoragesViewWidget from "@/widgets/storages-view";

const AvailableStoragesPage = (): ReactNode => {
    return (
        <div>
            <StoragesViewWidget />
        </div>
    );
};

export default AvailableStoragesPage;
