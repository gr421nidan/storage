const flexGap="flex  gap-[35px]"
const controlPanelContainerStyles = "flex flex-col gap-[40px]";

const headerStyles = "flex justify-between items-center mr-[17px]";

const buttonContainerStyles = `${flexGap} items-center`;

const buttonIconStyles = "h-[54px] w-[248px]";

const popupStyles = "absolute left-0 top-full mt-2 z-50";

const actionButtonStyles = "w-[295px] h-13";

const createFolderButtonStyles = "w-[261px] h-13";

export {
    createFolderButtonStyles,
    actionButtonStyles,
    buttonContainerStyles,
    buttonIconStyles,
    flexGap,
    popupStyles,
    headerStyles,
    controlPanelContainerStyles
}