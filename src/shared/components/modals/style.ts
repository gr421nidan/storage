const modalWrapperStyle =
    "fixed inset-0 z-50 flex items-center justify-center";

const backdropStyle = "fixed inset-0 backdrop-blur-md";

const modalPanelStyle =
    "relative bg-white flex flex-col gap-[28px] dark:bg-dark-theme dark:text-white rounded-[20px] py-[28px] px-[32px] border-2 border-purple dark:border-purple-light w-fit";

const modalTitleWrapperStyle = "flex justify-between items-end";

const modalTitleStyle = "text-[24px]";

const closeButtonStyle =
    "text-purple-light dark:text-purple cursor-pointer";

const separatorStyle = "bg-purple w-full h-[2px] dark:bg-purple-light";

export {
    modalWrapperStyle,
    backdropStyle,
    modalPanelStyle,
    modalTitleWrapperStyle,
    modalTitleStyle,
    closeButtonStyle,
    separatorStyle
};
